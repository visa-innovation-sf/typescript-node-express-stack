export enum DbTable {
  TABLE = 'table',
}

export enum DbTenancy {
  TENANT = 'tenant',
}

export enum ErrorMessage {
  MALFORM_INSERT_QUERY = 'Insert query is expecting at least 1 value',
}

export enum Log {
  ERROR = 'error.log',
  INFO = 'info.log',
  VERBOSE = 'verbose.log',
}

export enum LogLevel {
  DEBUG = 'debug',
  ERROR = 'error',
  INFO = 'info',
  VERBOSE = 'verbose',
}
